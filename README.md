# Secure Project Template

Private setup in progress. This repository will become a reusable, security-first,
AI-ready starter only after its implementation and verification gates pass.

No API key belongs in Git history. Local secrets must stay in ignored environment
files and AI calls must remain server-side.

